// import mongoose from 'mongoose'

export default async function userSeed () {
  // const testUser = { name: 'Test', email: 'test@test.com', password: 'secret' }
  // try {
  //   const response = await mongoose.model('User').findOne({ email: testUser.email })
  //   console.log(response)   
  // } catch (error) {
  //   console.log(error)
  // }
  // if (!_id)
}